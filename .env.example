# Base URL for the real backend once available.
# Until then, every feature reads from local mock data in src/mocks/data/*
# via TanStack Query, so the app runs fully standalone without this set.
VITE_API_BASE_URL=/api
